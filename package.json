{
  "name": "jupyterlab-extensions",
  "private": true,
  "scripts": {
    "preinstall": "npx npm-force-resolutions",
    "prepare": "npx lerna run clean && npx lerna run build"
  },
  "devDependencies": {
    "@babel/core": "^7.8.4",
    "@babel/preset-env": "^7.8.4",
    "@jupyterlab/testutils": "^1.2.2",
    "@types/jest": "^24.9.1",
    "jest": "^24.9.0",
    "lerna": "^3.20.2",
    "rimraf": "~3.0.0",
    "ts-jest": "^24.3.0",
    "typescript": "~3.7.3",
    "minimist": ">=1.2.3"
  },
  "engines": {
    "node": ">=10"
  }
}
